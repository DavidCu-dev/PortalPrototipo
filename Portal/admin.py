from django.contrib import admin
from .models import datosUsuarioM, declaratoriaPropiedadModel

# Register your models here.
admin.site.register(datosUsuarioM)
admin.site.register(declaratoriaPropiedadModel)
